import 'package:flutter/material.dart';
import 'package:pikseltesisat/product/utils/constants/app_sizes.dart';

final class AppPaddings {
  const AppPaddings._();

  /// Value: 0.0
  static const zero = EdgeInsets.zero;

  /// Value: 4.0
  static const allXXS = EdgeInsets.all(AppSizes.xxs);

  /// Value: 8.0
  static const allXS = EdgeInsets.all(AppSizes.xs);

  /// Value: 12.0
  static const allS = EdgeInsets.all(AppSizes.s);

  /// Value: 16.0
  static const allM = EdgeInsets.all(AppSizes.m);

  /// Value: 20.0
  static const allL = EdgeInsets.all(AppSizes.l);

  /// Value: 24.0
  static const allXL = EdgeInsets.all(AppSizes.xl);

  /// Value: 28.0
  static const allXXL = EdgeInsets.all(AppSizes.xxl);

  // Horizontal Paddings

  /// Value: horizontal: 4.0
  static const horizontalXXS = EdgeInsets.symmetric(horizontal: AppSizes.xxs);

  /// Value: horizontal: 8.0
  static const horizontalXS = EdgeInsets.symmetric(horizontal: AppSizes.xs);

  /// Value: horizontal: 12.0
  static const horizontalS = EdgeInsets.symmetric(horizontal: AppSizes.s);

  /// Value: horizontal: 16.0
  static const horizontalM = EdgeInsets.symmetric(horizontal: AppSizes.m);

  /// Value: horizontal: 20.0
  static const horizontalL = EdgeInsets.symmetric(horizontal: AppSizes.l);

  /// Value: horizontal: 24.0
  static const horizontalXL = EdgeInsets.symmetric(horizontal: AppSizes.xl);

  /// Value: horizontal: 28.0
  static const horizontalXXL = EdgeInsets.symmetric(horizontal: AppSizes.xxl);

  // Vertical Paddings

  /// Value: vertical: 4.0
  static const verticalXXS = EdgeInsets.symmetric(vertical: AppSizes.xxs);

  /// Value: vertical: 8.0
  static const verticalXS = EdgeInsets.symmetric(vertical: AppSizes.xs);

  /// Value: vertical: 12.0
  static const verticalS = EdgeInsets.symmetric(vertical: AppSizes.s);

  /// Value: vertical: 16.0
  static const verticalM = EdgeInsets.symmetric(vertical: AppSizes.m);

  /// Value: vertical: 20.0
  static const verticalL = EdgeInsets.symmetric(vertical: AppSizes.l);

  /// Value: vertical: 24.0
  static const verticalXL = EdgeInsets.symmetric(vertical: AppSizes.xl);

  /// Value: vertical: 28.0
  static const verticalXXL = EdgeInsets.symmetric(vertical: AppSizes.xxl);
}
