import 'package:flutter/material.dart';

final class PlumberListView extends StatelessWidget {
  const PlumberListView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.amberAccent);
  }
}
